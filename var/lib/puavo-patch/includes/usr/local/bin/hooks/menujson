#!/bin/sh

GRP=$(groups|sed 's/ /,/g')

if $(echo ${GRP} | grep -q "[lehrer|maint]"); then 
  echo "nothing to do"
  exit
fi

AMX="http://www.amxa.ch/puavo"

echo "wget -O ~/.config/webmenu/menu.json ${AMX}/menujson.php?groups=${GRP}"

exit
